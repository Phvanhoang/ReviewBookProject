package repository;

import entity.DistributorEntity;
import org.springframework.data.repository.CrudRepository;

public interface DistributorEntityRepository extends CrudRepository<DistributorEntity, Long> {
}
